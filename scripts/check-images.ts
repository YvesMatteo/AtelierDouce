import { supabase } from '../lib/supabase'; async function check() { const { data } = await supabase.from('products').select('*').limit(1); if (data && data[0]) console.log(JSON.stringify(data[0].images)); } check();
